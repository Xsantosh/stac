name: stac_export_builder
description: Code generator for exporting Flutter widgets to STAC JSON
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  analyzer: ^7.5.6
  build: ^2.5.4
  source_gen: ^2.0.0
  path: ^1.9.1
  args: ^2.7.0
  stac:
    path: ../stac

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.5.4
  flutter_lints: ^6.0.0

flutter:
